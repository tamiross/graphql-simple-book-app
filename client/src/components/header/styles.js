import styled from 'styled-components'

export const BaseStyle = styled.header({
    display: 'flex',
    justifyContent: 'space-between',
    padding: 12,
    background: '#1976D2',
    color: '#fff'
})
